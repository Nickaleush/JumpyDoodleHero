<?xml version="1.0" encoding="utf-8"?>
<!--
    Splash screen.
    The full artwork (logo, hero, background) is the supplied image
    `splash_background`. Only the loading progress bar is overlaid, pinned to
    the bottom of the screen as requested.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0F0A">

    <ImageView
        android:id="@+id/splashBackgroundImageView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="@string/splash_image_description"
        android:scaleType="centerCrop"
        android:src="@drawable/splash_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <FrameLayout
        android:id="@+id/progressContainer"
        android:layout_width="200dp"
        android:layout_height="14dp"
        android:layout_marginBottom="56dp"
        android:background="@drawable/bg_splash_progress_track"
        android:padding="2dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <View
            android:id="@+id/progressBarFillView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_splash_progress_fill"
            android:pivotX="0"
            android:scaleX="0" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
